crontab genertor
jenkins create an sh /python scripts and run it .
Trigger - at some time run the the script
can define what script run before, can add moere shell scripts one after the other (can be changed)


Jenkins master slave - same service that manage all what cause a load.
There is a need to setup a slave , sepreate the jobs, one machine manage the process, second server
SLAVE will run the scripts itself
Master and slave  - we split the manager and slave

Jenkins Pipline - pipline scripted or declearatiev (use jenkins plugin to do things)
jenkins dcleartive pipline for (something) - search in google (blugin built)